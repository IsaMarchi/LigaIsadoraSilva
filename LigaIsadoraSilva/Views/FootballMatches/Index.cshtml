@model IEnumerable<LigaIsadoraSilva.Data.Entities.FootballMatch>

@{
    ViewData["Title"] = "Matches";
}

<style>
    body {
        background-color: black;
        color: white;
    }

    .table {
        background-color: white;
        color: black;
    }

        .table th, .table td {
            color: black;
        }

    h1 {
        color: white;
    }

    .btn-primary {
        background-color: #66ff66;
        border-color: #66ff66;
        color: black;
        font-weight: bold;
    }

    .action-buttons {
        white-space: nowrap;
    }

        .action-buttons a {
            margin-right: 5px;
        }
</style>

<h1>Matches List</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">New Matche</a>
</p>

<table class="table table-striped table-bordered">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HomeTeam.Coach)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VisitTeam.Coach)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HomeGoals)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VisitGoals)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.StartDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HomeTeam.Coach)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.VisitTeam.Coach)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HomeGoals)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.VisitGoals)
                </td>
                <td>
                    <div class="action-buttons">
                        <a class="btn btn-sm" style="background-color: #00cccc; color: white;" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                        <a class="btn btn-sm" style="background-color: #99cc33; color: white;" asp-action="Details" asp-route-id="@item.Id">Details</a>
                        <a class="btn btn-sm" style="background-color: #ff6666; color: white;" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
